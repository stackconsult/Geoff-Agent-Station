# Tolaria Automation — Workflow Context

## Tech Stack

Rust (Tauri 2, tokio, serde, serde_yaml, walkdir, git2), TypeScript/React (Vite, Vitest, React Testing Library, BlockNote editor, Zustand planned), pnpm, GitHub Actions CI

## Conventions

Rust: async Tauri commands only, PathBuf for all path construction, backup-before-write safety contract on all file mutations (fs::write from in-memory content, not fs::copy), feature-gate scaffolding modules with #[cfg(feature)], no blanket #![allow(dead_code)] — fix warnings at source. cargo clippy -D warnings must be zero. TypeScript: invoke imported statically at top, no hardcoded paths, toast.info for stub handlers, stores for shared state (Zustand migration in Sprint 3). CI: windows-latest for Rust job, ubuntu-latest for frontend job, clippy gate blocks merge.

## Workflow Notes

- Sprint baseline commit: c7c0311
- Active branch: cascade/phase1-ui-overhaul (merged to master at 33ed1bc)
- Rust test count: 16 passing
- Frontend test count: 5 passing
- All gates green as of Sprint 2 completion (33ed1bc)
- Current sprint: F — TOCTOU backup fix, dead allow(dead_code) removal, CI dedup
- Next sprints: G (runtime smoke), H (test gap), I (Zustand migration)
- Known open issues:
  - update_frontmatter uses fs::copy (TOCTOU) — Sprint F F1
  - #![allow(dead_code)] still on 5 command files — Sprint F F2
  - Redundant cargo check step in CI — Sprint F F3
  - SystemMonitor.tsx has inline styles (lines 76, 103) — cosmetic, Sprint H or later
